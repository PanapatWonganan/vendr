# ⚠️ CLAUDE AI - อ่านก่อนทำงาน ⚠️

## ❌ ห้ามทำสิ่งเหล่านี้:
1. **ห้ามใช้ `migrate:fresh`** - จะลบข้อมูลทั้งหมด
2. **ห้ามใช้ `db:wipe`** - จะลบฐานข้อมูล
3. **ห้ามใช้ `migrate:refresh`** - ข้อมูลจะหาย

## ✅ ทำได้:
- `php artisan migrate` - เพิ่ม migration ใหม่
- `php artisan db:seed` - เพิ่มข้อมูลตัวอย่าง

## 🚨 User เตือนแล้ว 2 ครั้ง - อย่าทำผิดอีก!

ข้อมูลในฐานข้อมูลสำคัญมาก มีข้อมูลการทำงานหลายเดือน