<x-filament-widgets::widget>
    <x-filament::section>
        <x-slot name="heading">
            📅 ปฏิทินกำหนดส่งมอบ
        </x-slot>
        
        <x-slot name="headerEnd">
            <div class="flex gap-4 text-sm text-gray-600">
                <span class="flex items-center gap-1">
                    <span class="w-3 h-3 bg-red-500 rounded-full"></span>
                    เร่งด่วน
                </span>
                <span class="flex items-center gap-1">
                    <span class="w-3 h-3 bg-orange-500 rounded-full"></span>
                    สำคัญ
                </span>
                <span class="flex items-center gap-1">
                    <span class="w-3 h-3 bg-yellow-500 rounded-full"></span>
                    ปกติ
                </span>
                <span class="flex items-center gap-1">
                    <span class="w-3 h-3 bg-green-500 rounded-full"></span>
                    ปลอดภัย
                </span>
            </div>
        </x-slot>
        
        <div class="space-y-4">
            <!-- Summary Stats -->
            <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
                <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-sm text-blue-600 font-medium">Purchase Orders</p>
                            <p class="text-2xl font-bold text-blue-700">{{ $totalPOs }}</p>
                        </div>
                        <div class="text-blue-500">
                            <svg class="w-8 h-8" fill="currentColor" viewBox="0 0 20 20">
                                <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                            </svg>
                        </div>
                    </div>
                </div>
                
                <div class="bg-green-50 border border-green-200 rounded-lg p-4">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-sm text-green-600 font-medium">Purchase Requisitions</p>
                            <p class="text-2xl font-bold text-green-700">{{ $totalPRs }}</p>
                        </div>
                        <div class="text-green-500">
                            <svg class="w-8 h-8" fill="currentColor" viewBox="0 0 20 20">
                                <path d="M8 4a4 4 0 100 8 4 4 0 000-8zM2 8a6 6 0 1110.89 3.476l4.817 4.817a1 1 0 01-1.414 1.414l-4.816-4.816A6 6 0 012 8z"/>
                            </svg>
                        </div>
                    </div>
                </div>
                
                <div class="bg-red-50 border border-red-200 rounded-lg p-4">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-sm text-red-600 font-medium">เร่งด่วน</p>
                            <p class="text-2xl font-bold text-red-700">{{ $urgentCount }}</p>
                        </div>
                        <div class="text-red-500">
                            <svg class="w-8 h-8" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
                            </svg>
                        </div>
                    </div>
                </div>
                
                <div class="bg-purple-50 border border-purple-200 rounded-lg p-4">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-sm text-purple-600 font-medium">เดือนนี้</p>
                            <p class="text-2xl font-bold text-purple-700">{{ $currentMonth->format('M Y') }}</p>
                        </div>
                        <div class="text-purple-500">
                            <svg class="w-8 h-8" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M6 2a1 1 0 00-1 1v1H4a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-1V3a1 1 0 10-2 0v1H7V3a1 1 0 00-1-1zm0 5a1 1 0 000 2h8a1 1 0 100-2H6z" clip-rule="evenodd"/>
                            </svg>
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Calendar Grid -->
            <div class="bg-white rounded-lg border">
                <div class="grid grid-cols-7 border-b">
                    @foreach(['อา', 'จ', 'อ', 'พ', 'พฤ', 'ศ', 'ส'] as $day)
                        <div class="p-3 text-center text-sm font-medium text-gray-700 border-r last:border-r-0">
                            {{ $day }}
                        </div>
                    @endforeach
                </div>
                
                @php
                    $startOfMonth = $currentMonth->copy()->startOfMonth();
                    $endOfMonth = $currentMonth->copy()->endOfMonth();
                    $startOfCalendar = $startOfMonth->copy()->startOfWeek(\Illuminate\Support\Carbon::SUNDAY);
                    $endOfCalendar = $endOfMonth->copy()->endOfWeek(\Illuminate\Support\Carbon::SATURDAY);
                    
                    $current = $startOfCalendar->copy();
                    $weeks = [];
                    
                    while ($current <= $endOfCalendar) {
                        $week = [];
                        for ($i = 0; $i < 7; $i++) {
                            $week[] = $current->copy();
                            $current->addDay();
                        }
                        $weeks[] = $week;
                    }
                @endphp
                
                @foreach($weeks as $week)
                    <div class="grid grid-cols-7">
                        @foreach($week as $day)
                            @php
                                $dateKey = $day->format('Y-m-d');
                                $dayEvents = $events[$dateKey] ?? collect();
                                $isCurrentMonth = $day->month === $currentMonth->month;
                                $isToday = $day->isToday();
                            @endphp
                            
                            <div class="border-r last:border-r-0 border-b min-h-[120px] p-1 
                                {{ !$isCurrentMonth ? 'bg-gray-50' : '' }}
                                {{ $isToday ? 'bg-blue-50 border-blue-200' : '' }}">
                                
                                <!-- Date Number -->
                                <div class="flex justify-between items-center mb-1">
                                    <span class="text-sm font-medium 
                                        {{ !$isCurrentMonth ? 'text-gray-400' : 'text-gray-700' }}
                                        {{ $isToday ? 'text-blue-600 font-bold' : '' }}">
                                        {{ $day->format('j') }}
                                    </span>
                                    
                                    @if($dayEvents->isNotEmpty())
                                        <span class="bg-red-100 text-red-600 text-xs px-1 rounded-full">
                                            {{ $dayEvents->count() }}
                                        </span>
                                    @endif
                                </div>
                                
                                <!-- Events -->
                                <div class="space-y-1">
                                    @foreach($dayEvents->take(3) as $event)
                                        <a href="{{ $event['url'] }}" 
                                           class="block text-xs p-1 rounded 
                                           {{ $event['color'] === 'red' ? 'bg-red-100 text-red-700 hover:bg-red-200' : '' }}
                                           {{ $event['color'] === 'orange' ? 'bg-orange-100 text-orange-700 hover:bg-orange-200' : '' }}
                                           {{ $event['color'] === 'yellow' ? 'bg-yellow-100 text-yellow-700 hover:bg-yellow-200' : '' }}
                                           {{ $event['color'] === 'green' ? 'bg-green-100 text-green-700 hover:bg-green-200' : '' }}">
                                            
                                            <div class="font-medium truncate">{{ $event['title'] }}</div>
                                            <div class="text-xs opacity-75 truncate">{{ $event['subtitle'] }}</div>
                                            
                                            @if($event['amount'])
                                                <div class="text-xs font-medium">{{ $event['amount'] }} บาท</div>
                                            @endif
                                            
                                            @if(isset($event['days_until']) && $event['days_until'] >= 0)
                                                <div class="text-xs opacity-60">อีก {{ $event['days_until'] }} วัน</div>
                                            @endif
                                        </a>
                                    @endforeach
                                    
                                    @if($dayEvents->count() > 3)
                                        <div class="text-xs text-gray-500 p-1">
                                            +{{ $dayEvents->count() - 3 }} เพิ่มเติม
                                        </div>
                                    @endif
                                </div>
                            </div>
                        @endforeach
                    </div>
                @endforeach
            </div>
            
            <!-- Upcoming Events List -->
            @php
                $upcomingEvents = $events->flatten(1)
                    ->filter(function($event) {
                        return isset($event['days_until']) && $event['days_until'] >= 0;
                    })
                    ->sortBy('days_until')
                    ->take(5);
            @endphp
            
            @if($upcomingEvents->isNotEmpty())
                <div class="bg-white rounded-lg border p-4">
                    <h3 class="text-lg font-semibold mb-3">🔔 กำหนดการที่ใกล้เข้ามา</h3>
                    
                    <div class="space-y-3">
                        @foreach($upcomingEvents as $event)
                            <div class="flex items-center justify-between p-3 rounded-lg border 
                                {{ $event['color'] === 'red' ? 'border-red-200 bg-red-50' : '' }}
                                {{ $event['color'] === 'orange' ? 'border-orange-200 bg-orange-50' : '' }}
                                {{ $event['color'] === 'yellow' ? 'border-yellow-200 bg-yellow-50' : '' }}
                                {{ $event['color'] === 'green' ? 'border-green-200 bg-green-50' : '' }}">
                                
                                <div class="flex-1">
                                    <div class="flex items-center gap-2">
                                        <h4 class="font-medium">{{ $event['title'] }}</h4>
                                        
                                        @if($event['days_until'] <= 1)
                                            <span class="bg-red-100 text-red-600 text-xs px-2 py-1 rounded-full font-medium">
                                                เร่งด่วน
                                            </span>
                                        @elseif($event['days_until'] <= 7)
                                            <span class="bg-orange-100 text-orange-600 text-xs px-2 py-1 rounded-full font-medium">
                                                สำคัญ
                                            </span>
                                        @endif
                                    </div>
                                    
                                    <p class="text-sm text-gray-600">{{ $event['subtitle'] }}</p>
                                    
                                    @if($event['amount'])
                                        <p class="text-sm font-medium">{{ $event['amount'] }} บาท</p>
                                    @endif
                                </div>
                                
                                <div class="text-right">
                                    <div class="text-sm font-medium 
                                        {{ $event['color'] === 'red' ? 'text-red-600' : '' }}
                                        {{ $event['color'] === 'orange' ? 'text-orange-600' : '' }}
                                        {{ $event['color'] === 'yellow' ? 'text-yellow-600' : '' }}
                                        {{ $event['color'] === 'green' ? 'text-green-600' : '' }}">
                                        {{ $event['display_date'] }}
                                    </div>
                                    
                                    <div class="text-xs text-gray-500">
                                        @if($event['days_until'] == 0)
                                            วันนี้
                                        @elseif($event['days_until'] == 1)
                                            พรุ่งนี้
                                        @else
                                            อีก {{ $event['days_until'] }} วัน
                                        @endif
                                    </div>
                                </div>
                            </div>
                        @endforeach
                    </div>
                </div>
            @endif
        </div>
    </x-filament::section>
</x-filament-widgets::widget>